# 第一章与第二章终稿完成版

本分支按照以下边界整理：

- 第一章中“现有研究不足与本文研究思路”和“本文主要研究内容与章节安排”保持冻结，待第三章至第五章完成后再与摘要、结论与展望统一确认。
- 第一章其余内容已结合最初版和新版进行重组，补充了代表性引用，统一了“含细胞复合液滴”、圆度、圆度偏差和黏弹性特征时间等表述。
- 第二章采用统一物理与数值框架组织两个数值模拟，在流体、界面、细胞材料、流固耦合、后处理和参数反演各环节中自然说明二者差异。

## 文件

- `chapters/chap01_final.tex`
- `chapters/chap02_final.tex`
- `figures/chap02/*.tex`：可直接由章节文件通过 `\input` 调用的标准 LaTeX 图件
- `figures/chap02/*.svg`：相同图件的浏览器预览和后续重绘参考

第一章三幅文献图继续使用原工程中的下列文件：

- `figures/chap01/fig_rtdc_mietke2015.png`
- `figures/chap01/fig_rtdc_mokbel2017.png`
- `figures/chap01/fig_drop_impact_pasandideh1996.png`

## 参考文献

章节引用键按照用户现有的 `参考文献数据库_前四章_可封存终稿完成版.bib` 编写。将该文件复制为主工程中的 `reference/refs.bib`，或在主文件中直接加载其实际路径。不要同时加载含有重复引用键的多个 BibTeX 文件。

参考文献数据库仍需在全文最终合并时完成以下整理：

1. 删除同一 VAE 文献的重复预印本与会议条目，只保留 `kingma2014vae`。
2. 将 BibTeX 中使用 `and others` 截断的作者字段恢复为完整作者名单，由学校样式控制最终显示数量。
3. 将引用键年份与正式出版年份不一致的键统一重命名，例如 `gavara2016afm` 可在全文一次性改为 `gavara2017afm`。当前章节暂保留旧键，以避免其他章节交叉引用失效。
4. 非 arXiv 文献不保留 DOI、URL 和访问日期。正式发表版本优先于预印本。

## 第二章的关键校正

- 明确微通道模型中的固体域为有限厚度连续等效细胞膜层，复合液滴模型中的固体域为有限体积内部细胞。
- 将第四章采用的黏弹性参数关系统一为 `eta_s = E tau_v`，并补充其与剪切特征时间的区别。
- 给出壁面自由能、平衡接触角和润湿边界条件的完整关系。
- 在相场辅助方程弱形式中保留润湿边界积分项。
- 统一相场变量为 `varphi`。
- 明确圆度 `C` 和圆度偏差 `D_c=1-C`，不再与其他变形指数混用。
- 补充卷积运算、过程级数据划分、总体相对误差和 MRE@95% 的定义。
- 重新绘制图2.1至图2.4，删除所有“临时示意”和“终稿阶段替换”说明。

## 集成检查

合并至原始上海大学学位论文工程后，应进行一次干净编译，并重点检查：

- `\input` 路径与图片路径
- 第三章和第四章对第二章公式标签的调用
- `\eta_{\mathrm s}=E\tau_{\mathrm v}` 在第四章中的一致性
- 动态接触角拟合窗口和采样点数是否已在第四章明确给出
- 全文符号表中的 `varphi`、`psi`、`C`、`D_c`、`tau_v` 和 `eta_s`
- 冻结的两个第一章小节是否保持不变
